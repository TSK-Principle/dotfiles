User-1:
``````text
请教我aws云数据库(我是云数据库初学者,我没有钱也没有信用卡去启动免费的云套餐,但是朋友告诉我可以用aws-cli和localstack,我用的哥哥的电脑他自己装的debian),我希望类似codecademy那样的极小微代码项目每个小节,(一开始讲解然后是作业,对于作业请给我每一步要做什么风格就像codecademy一样,它的作业风格类似一个作业拆分成多个检查点每个检查点都有明确的指令指向去引导我完成,每个小节完成了我把作业给你检查,你不要一小节太多东西,我的水平很差可能接受不了,我们开始第一个小节, 什么时候毕业你下定论
``````
Model-1:
``````markdown
太棒了！欢迎来到你的“零成本”AWS云数据库第一课。你的朋友给的建议非常专业——**LocalStack** 是一个在本地电脑模拟AWS环境的神器，配合 **AWS CLI**（命令行工具），你完全不需要花一分钱，就能在哥哥的Debian电脑上学会操作云数据库。

既然你的水平是初学者，我们会切分得很细。我是你的AI导师。我会用**Codecademy**的风格：**先懂原理，再动手，最后检查**。

---

### 第1节：搭建你的“云”控制台

#### 🧠 知识点：这是在做什么？

想象一下，AWS（亚马逊云）是一台远在天边的超级计算机。通常我们需要登录网页去点击按钮操作它。
但是，真正的云专家（比如未来的你）更喜欢用键盘指令来控制它，这叫 **CLI (Command Line Interface)**。

因为我们没有钱买云服务，所以我们要在你的电脑里装一个“假的”AWS（这就是 **LocalStack**），然后用 **AWS CLI** 这个“遥控器”去指挥它。

本节目标：**安装好“遥控器”（AWS CLI）并确保你的环境能运行“假云”（LocalStack）。**

---

#### 💻 实战作业 (Mission 1)

请打开你哥哥电脑上的 **Terminal (终端)**，按顺序完成以下3个检查点 (Checkpoints)。

##### ☑️ 检查点 1：检查地基 (Check Prerequisites)

LocalStack 运行需要 Python 和 Docker。因为是哥哥的电脑，可能已经装好了。我们先检查一下。

**指令：** 请在终端逐行复制并运行以下命令：

```bash
python3 --version
docker --version

```

> *如果不报错并显示了版本号（比如 `Python 3.x.x` 和 `Docker version...`），说明地基是好的。如果提示 `command not found`，请立刻告诉我。*

##### ☑️ 检查点 2：安装 AWS CLI (The Remote Control)

我们需要安装这个“遥控器”。在 Debian 上，最简单的办法是用 `apt` 或者 `curl`。为了保证版本够新，我们用官方推荐的 `curl` 方式（别怕，直接复制粘贴）。

**指令：** 1. 下载并安装（可能需要输入你哥哥电脑的密码）：

```bash
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

```

2. 验证是否安装成功：

```bash
aws --version

```

> *如果看到类似 `aws-cli/2.x.x ...` 的字样，恭喜你，遥控器到手了！*

##### ☑️ 检查点 3：安装 LocalStack (The Fake Cloud)

我们需要用 Python 的包管理器 `pip` 来安装 LocalStack 的控制端。

**指令：**

```bash
pip install localstack

```

*(注意：如果提示权限不足，可能需要加 `sudo` 或者用 `pip3`)*

##### ☑️ 检查点 4：配置假身份 (Configuration)

AWS CLI 默认需要你输入账号密码。因为我们用的是本地模拟器，我们可以随便填一个“假”的，主要是为了骗过软件的检查，让它能跑起来。

**指令：** 运行下面的命令：

```bash
aws configure

```

**系统会问你4个问题，请按下面的内容填（直接复制粘贴）：**

1. `AWS Access Key ID`: **test** (按回车)
2. `AWS Secret Access Key`: **test** (按回车)
3. `Default region name`: **us-east-1** (按回车)
4. `Default output format`: **json** (按回车)

---

### 📤 提交作业

请把 **检查点 1** 和 **检查点 2** 的输出结果（就是屏幕上显示的版本号）复制粘贴发给我。

如果中间任何一步报错（比如全是红色的字或者说找不到命令），请直接把报错信息发给我，不要勉强往下做。等你提交，我们再决定要不要开始下一节！
``````

---

User-2:
``````text

~/cloud via py v3.13.5 (.venv) on aws (us-east-1) took 2s
> python3 --version
  docker --version
Python 3.13.5
Docker version 29.2.1, build a5c7197

~/cloud via py v3.13.5 (.venv) on aws (us-east-1)
> aws --version
aws-cli/2.23.6 Python/3.13.5 Linux/6.12.63+deb13-amd64 source/x86_64.debian.13

~/cloud via py v3.13.5 (.venv) on aws (us-east-1)
>
``````
Model-2:
``````markdown
太棒了！你的环境配置非常完美，版本都很新。这说明你的基础打得很牢（或者你哥哥的电脑维护得很好 😉）。

既然“遥控器” (AWS CLI) 已经拿在手里了，现在我们需要把那台“假云服务器” (LocalStack) 开机。

---

### 第2节：启动引擎与第一次连接

#### 🧠 知识点：我们要连接哪里？

在真实的 AWS 工作中，当你敲下命令时，指令会通过互联网飞到亚马逊的数据中心。
但在我们的课里，我们需要把指令发送到 **`localhost` (你自己电脑的本地端口)**。

LocalStack 本质上是一个运行在 Docker 里的容器，它会在你电脑的 **4566 端口** 监听你的指令。

本节目标：**启动 LocalStack，并验证你的“遥控器”能不能成功连上它。**

---

#### 💻 实战作业 (Mission 2)

请继续在终端中操作。

##### ☑️ 检查点 1：启动 LocalStack (Ignition)

我们需要让 LocalStack 在后台运行，这样它就不会占用你现在的终端窗口。我们要用到 `-d` (Detached，分离模式) 参数。

**指令：**

```bash
localstack start -d

```

> *系统可能会提示你下载 Docker 镜像（第一次启动会慢一点，因为要下载大约几百MB的数据），请耐心等待直到它显示类似 `Ready.` 或者回到命令行提示符。*

##### ☑️ 检查点 2：确认服务状态 (System Check)

启动后，我们要确保“数据库服务”已经准备好为我们服务了。

**指令：**

```bash
localstack status services

```

> *你会看到一个长列表。请观察输出中是否有 **`dynamodb`** 这一项，并且状态显示为 `running` 或 `available`。*
> *(DynamoDB 是我们将要学习的第一个 AWS 数据库，它是亚马逊最经典的 NoSQL 数据库)*

##### ☑️ 检查点 3：Hello World (第一次握手)

这是最关键的一步。我们要试着命令 AWS CLI 去“问候”一下 LocalStack。
**注意：** 这一步必须加上 `--endpoint-url` 参数，明确告诉 CLI 别去连亚马逊官网，而是连我们本地的模拟器。

**指令：**
尝试列出当前的数据库表（虽然现在应该是空的）：

```bash
aws dynamodb list-tables --endpoint-url=http://localhost:4566

```

---

### 📤 提交作业

1. **检查点 2** 的输出中，`dynamodb` 的状态是什么？
2. **检查点 3** 运行后，如果成功，它应该会输出一段 JSON 格式的代码（哪怕是空的）。请把那段输出复制给我。

*(如果检查点 3 报错说 `Connection refused`，说明 LocalStack 还没启动完全，请等 30 秒再试一次。)*
``````

---

User-3:
``````text

~/cloud via py v3.13.5 (.venv) on aws (us-east-1) took 5s
> localstack status services
┏━━━━━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━┓
┃ Service                  ┃ Status      ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━┩
│ acm                      │ ✔ available │
│ apigateway               │ ✔ available │
│ cloudformation           │ ✔ available │
│ cloudwatch               │ ✔ running   │
│ config                   │ ✔ available │
│ dynamodb                 │ ✔ running   │
│ dynamodbstreams          │ ✔ available │
│ ec2                      │ ✔ available │
│ es                       │ ✔ available │
│ events                   │ ✔ available │
│ firehose                 │ ✔ available │
│ iam                      │ ✔ available │
│ kinesis                  │ ✔ available │
│ kms                      │ ✔ available │
│ lambda                   │ ✔ running   │
│ logs                     │ ✔ running   │
│ opensearch               │ ✔ available │
│ redshift                 │ ✔ available │
│ resource-groups          │ ✔ available │
│ resourcegroupstaggingapi │ ✔ available │
│ route53                  │ ✔ available │
│ route53resolver          │ ✔ available │
│ s3                       │ ✔ running   │
│ s3control                │ ✔ available │
│ scheduler                │ ✔ available │
│ secretsmanager           │ ✔ available │
│ ses                      │ ✔ available │
│ sns                      │ ✔ available │
│ sqs                      │ ✔ available │
│ ssm                      │ ✔ available │
│ stepfunctions            │ ✔ available │
│ sts                      │ ✔ running   │
│ support                  │ ✔ available │
│ swf                      │ ✔ available │
│ transcribe               │ ✔ available │
└──────────────────────────┴─────────────┘

~/cloud via py v3.13.5 (.venv) on aws (us-east-1)
< aws dynamodb list-tables --endpoint-url=http://localhost:4566
{
    "TableNames": []
}

~/cloud via py v3.13.5 (.venv) on aws (us-east-1)
>
``````
Model-3:
``````markdown
太棒了！🎉 你的作业完成得非常完美。

**看这里：**

1. `dynamodb` 显示 `✔ running`，说明数据库引擎已经在转动了。
2. `"TableNames": []` 虽然是空的，但这正是我们要的结果——说明你成功连上了数据库，只是里面还没有东西。

现在，我们正式开始操作数据库！

---

### 第3节：创建你的第一张“表” (Create Table)

#### 🧠 知识点：我们要存什么？

在云数据库（特别是 **DynamoDB**，我们要学的这个）里，数据是存放在**表 (Table)** 里的。
你可以把“表”想象成一个 Excel 文件。

但是，DynamoDB 既然是“NoSQL”（非关系型）数据库，它和 Excel 有一个巨大的不同：

* **Excel**：你必须先要把每一列的名字起好（姓名、年龄、电话...），然后才能填数据。
* **DynamoDB**：你**不需要**提前定义所有列！你只需要定义**唯一能代表身份的那一列**。
* 这个“身份证列”在 AWS 里叫 **主键 (Primary Key)**。



**本节目标**：创建一个用来存用户信息的表，表名叫 `Users`。
**规则**：我们规定每个用户必须有一个 `Username`（用户名），这个就是我们的**主键**。

---

#### 💻 实战作业 (Mission 3)

请按照步骤，在终端里输入命令。

##### ☑️ 检查点 1：编写建表指令

这个命令有点长，因为我们要告诉 AWS 很多细节。别被吓到，我把它拆解了，你只需要**完整复制**下面的代码块并在终端运行。

*(注意：命令里的 `\` 符号告诉电脑“这句话还没说完，换行继续读”，所以在终端里粘贴后，如果它没自动运行，请按一下回车键)*

**指令：**

```bash
aws dynamodb create-table \
    --table-name Users \
    --attribute-definitions AttributeName=Username,AttributeType=S \
    --key-schema AttributeName=Username,KeyType=HASH \
    --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5 \
    --endpoint-url=http://localhost:4566

```

> **代码解密（不用背，看懂就行）：**
> * `--table-name Users`: 表名叫 Users。
> * `--attribute-definitions`: 定义属性。我们说 `Username` 是 `S` (String，字符串/文字的意思)。
> * `--key-schema`: 指定主键。我们选定 `Username` 作为主键（HASH 是 AWS 的术语，意思是主要索引）。
> * `--endpoint-url`: **永远别忘了这个**，告诉它连到 LocalStack。
> 
> 

##### ☑️ 检查点 2：验证表是否存在

刚才那个命令运行后，屏幕上会冒出一大段 JSON 信息（描述了表的细节）。现在我们要确认它真的在那儿。

还记得上一节我们用的“列出所有表”的命令吗？请再运行一次。

**指令：**

```bash
aws dynamodb list-tables --endpoint-url=http://localhost:4566

```

---

### 📤 提交作业

请告诉我：

1. 在运行 **检查点 1** 后，终端有没有报错？
2. 在运行 **检查点 2** 后，现在的输出变成了什么？（以前是 `[]`，现在应该不一样了）

*(把结果复制粘贴给我就行，如果看到 `"Users"` 字样，你就成功创建了人生中第一个云数据库表！)*
``````

---

